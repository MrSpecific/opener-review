import Layout from '@components/layout/Layout';
import styles from '../styles/Home.module.css';

export default function Home() {
  return <Layout className={styles.container} pageTitle="The Only Review in the Game"></Layout>;
}
